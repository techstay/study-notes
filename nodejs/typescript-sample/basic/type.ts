// date
const now = new Date()
console.log(now)
console.log(typeof now)

// array
const numbers = [1, 2, 3, 4, 5]
console.log(typeof numbers)
console.log(numbers)

// tuple
const pair: [string, number] = ['john', 3]
console.log(pair)

// set
const uniqueNumbers = new Set([1, 2, 3, 3, 2, 1])
console.log(uniqueNumbers)

// map
const map = new Map([['a', 1], ['b', 2], ['c', 3]])
console.log(map)
